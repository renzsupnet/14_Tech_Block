<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link href="/style.css" rel="stylesheet">
</head>
<body>
  <div class="d-flex flex-column min-vh-100">
    <header class="d-flex justify-content-between align-items-center p-2 text-white bg-dark">
      <h1 class="mb-0">
        <a href="/" class="link-light link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover mx-1">The Tech Blog</a>
      </h1>
      <nav class="d-flex align-items-center">
        <!-- Conditionally render login or logout links -->
        <a href="/" class="link-light link-offset-2 link-underline-opacity-25 link-underline-opacity-0-hover mx-1">Home</a>
        {{#if logged_in}}
        <a href="/profile" class="link-light link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover mx-1">Dashboard</a>
        <button type="button" class="btn btn-danger ms-2" id="logout">Logout</button>
        {{else}}
        <a href="/profile" class="link-light link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover mx-1">Dashboard</a>
        <a href="/login" class="btn btn-primary ms-2">Login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5 flex-grow-1">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="bg-dark text-white text-center py-3">
      <!-- Footer content here -->
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>
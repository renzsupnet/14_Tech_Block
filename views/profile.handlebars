<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{username}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Blog Post:</h3>

  <div class="container">
    <form class="form new-blogPost-form">
      <div class="mb-3">
        <label for="blogPost-title" class="form-label">Title:</label>
        <input class="form-control" type="text" id="blogPost-title" name="blogPost-title" />
      </div>
      <div class="mb-3">
        <label for="blogPost-desc" class="form-label">Description:</label>
        <textarea class="form-control" id="blogPost-desc" name="blogPost-desc"></textarea>
      </div>
      <div class="mb-3">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>
  </div>

  {{#if blogPosts.length}}
  <div class="col-md-6 blogPost-list">
    <h3>Current Blog Posts:</h3>

    {{#each blogPosts as |blogPost|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4 style="color: black;"><a href="/blogPosts/{{blogPost.id}}">{{blogPost.title}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{blogPost.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/profile.js"></script>
